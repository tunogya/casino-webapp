import { defineStyleConfig } from '@chakra-ui/react'

const Text = defineStyleConfig({
  baseStyle: {
    fontWeight: 400,
    color: "white",
    fontFamily: 'Readex Pro',
  },
})

export default Text