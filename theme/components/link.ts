import { defineStyleConfig } from '@chakra-ui/react'

const Link = defineStyleConfig({
  baseStyle: {
    fontFamily: 'Readex Pro',
    fontWeight: 400,
    color: "white",
  },
})

export default Link